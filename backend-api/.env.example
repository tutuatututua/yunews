# Backend-only environment (safe to commit)
# Get these from Supabase Dashboard → Project Settings → API

SUPABASE_URL=https://YOUR-PROJECT-REF.supabase.co

# Choose ONE key option.
#
# Option A (recommended when Supabase RLS is enabled and you don't have policies yet):
# Service role bypasses RLS. Keep it server-side only.
SUPABASE_SERVICE_ROLE_KEY=YOUR_SUPABASE_SERVICE_ROLE_KEY

# Option B (only if you have RLS policies for the tables this API reads/writes):
# IMPORTANT: Use the anon public JWT key (typically starts with eyJ...)
# SUPABASE_ANON_KEY=YOUR_SUPABASE_ANON_JWT_KEY

# Runtime environment
ENV=development
LOG_LEVEL=INFO

# CORS
# In production, set an explicit allowlist (comma-separated) matching your frontend origin(s).
# Example: CORS_ALLOW_ORIGINS=https://yunews.example.com
CORS_ALLOW_ORIGINS=http://localhost:5173

# Optional hardening: lock Host header to known values.
# Example: TRUSTED_HOSTS=yunews.example.com,api.yunews.example.com
TRUSTED_HOSTS=
